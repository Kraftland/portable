[package]
name = "portable-daemon"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
legacy_conf = { path = "./lib/legacy_conf" }

[workspace]
resolver = "3"
members = ["lib/legacy_conf", "lib/init"]
